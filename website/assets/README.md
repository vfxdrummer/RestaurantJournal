# Screenshots for the website

Drop your 5 App Store images into this `assets/` folder with these exact filenames.
(These are the images you pasted into chat — map each one to the filename below.)

| Filename          | Which image                                                        |
|-------------------|--------------------------------------------------------------------|
| `hero.png`        | The **brand poster** — "Remember every meal that mattered." (logo + plated food) |
| `scan.png`        | **"Scan your photos. Find every place you dined."** (journal list, "Private by design") |
| `map-local.png`   | **"See every dining memory on the map."** (zoomed-in street map)    |
| `map-world.png`   | **"Your food journey, all around the world."** (world / continent map) |
| `rewards.png`     | **"Rewards you earned. Programs you missed."** (Rewards list)       |

Notes
- Keep them as `.png` (or update the `src=` in `index.html` if you use `.jpg`).
- The App Store panels are tall (~9:19.5) — that's fine; the layout handles the aspect ratio.
- After adding the files, open `index.html` locally to confirm they all appear before deploying.
